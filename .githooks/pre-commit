#!/bin/sh
# 检测提交中是否包含私钥格式的字符串 (0x + 64位hex)
if git diff --cached --diff-filter=ACM | grep -qE '0x[0-9a-fA-F]{64}'; then
    echo "⚠️  检测到疑似私钥！请检查提交内容。"
    echo "   如果确认安全，使用 git commit --no-verify 跳过检查。"
    exit 1
fi
